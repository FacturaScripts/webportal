{% extends "Master/PortalTemplate.html.twig" %}

{% block body %}
    <div class="body-section">
        <div class="container grid-lg">
            <div class="columns">
                <div class="column">
                    <div class="empty">
                        <div class="empty-icon">
                            <i class="fas fa-lock fa-5x" aria-hidden="true"></i>
                        </div>
                        <h1 class="empty-title h4">{{ i18n.trans('access-denied') }}</h1>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}